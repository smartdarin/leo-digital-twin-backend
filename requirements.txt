# Core API
flask==3.0.3
gunicorn==21.2.0
flask-cors==4.0.0
requests==2.32.3
python-dotenv==1.0.1  # for local dev only

# Lightweight math / data (common for preprocessing)
numpy==1.26.4
pandas==2.2.2
Pillow==10.4

# HTTP robustness (timeouts/retries)
urllib3==2.2.2
httpx==0.27.2         # optional; if using async calls

# Optional: geospatial if backend actually computes geo ops
shapely==2.0.4        # geometry ops
pyproj==3.6.1         # CRS transforms
